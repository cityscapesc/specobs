@model  IEnumerable<Microsoft.Spectrum.Storage.Question>

@if(Model.Count() > 0)
{
    foreach(var question in Model)
    {
        <section>
            <h3>Q. @question.Ask</h3>
            <p style="white-space: pre-line">@question.Answer</p>
        </section>
    }
}
